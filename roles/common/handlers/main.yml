---
- name: restart networking
  ansible.builtin.service: 
    name: "{{ item }}"
    state: restarted
  loop:
    - networking
    - dhcpcd

- name: restart timezone
  ansible.builtin.service: 
    name: "{{ item }}"
    state: restarted
  loop: "{{ pifarm_timezone_services }}"
  